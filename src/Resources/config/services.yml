services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true

    ContaoEstateManager\RegionEntity\:
        resource: ../../
        exclude: ../../{ContaoManager,Model,Resources}
